# Configuration

- **config.yaml**: Manages global settings like audio mix levels (`vocal_mix_volume`, `music_mix_volume`) and valid file extensions.
- **Defaults**: If `config.yaml` is missing, the script defaults to 1.0 mix volumes and standard video extensions (`.mp4`, `.mkv`, `.avi`, `.mov`).
